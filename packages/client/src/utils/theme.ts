import { createTheme } from '@nextui-org/react';

export const theme = createTheme({
  type: 'dark',
  theme: {
    colors: {
      primary: '#0dcb21',
    },
    fonts: {
      lato: "'Lato','sans-serif'",
    },
  },
});
