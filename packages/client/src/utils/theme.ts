import { createTheme } from '@nextui-org/react';

export const theme = createTheme({
  type: 'dark',
  theme: {
    colors: {
      primaryGreen: '#0dcb21',
    },
    fonts: {
      lato: "'Lato','sans-serif'",
    },
  },
});
